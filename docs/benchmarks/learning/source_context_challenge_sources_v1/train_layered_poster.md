# Synthetic Source Packet G

A3 poster source with semantic layers, z_index notes, and separated foreground accents.

Production signal: rewrite the prompt so generation follows the declared layer ordering.
