# Vulca PPT Skill

Status: not-run.

Run 1.5 workflow:

1. Read the commercial brief and extract audience, decision, constraints, and output promise.
2. Read source summaries only as reference analysis and preserve do-not-copy boundaries.
3. Read tutorial notes and convert hierarchy, product-surface, proof-object, and QA rules into design-memory observations.
4. Validate `design_memory.json` against the strict Run 1.5 contract before generation.
5. Generate the deck from `deck_outline.json`, `slide_patterns.json`, `style_tokens.json`, and `asset_rules.json`.
6. Keep all core proof objects editable as native shapes, text, tables, or simple vectors.
7. Run comparison, ablation, render, provenance, and human review gates before any public publishing decision.

This workflow makes no post-training claim. It tests case-pack-controlled generation at run time, not model training or fine-tuning.
