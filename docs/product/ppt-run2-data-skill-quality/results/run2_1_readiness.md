# Run 2.1 Rerun Result

Status: rerun_completed_public_blocked.

Run 2.1 is not a new stage. It thickened the same Run 2 product layers and then reran the four-arm experiment:

- source and video cards now include executable `extraction_units`;
- `skill_workflow.json` records the declarative stage contract, gates, and human-gated repair triggers;
- `scripts/refresh_ppt_trace_qa.py` can refresh local trace QA outcomes with dry-run, backup, and atomic replacement.

The trace refresh utility is a prerequisite helper, not release evidence by itself. In this pass it was applied to the regenerated Run 2.1 arms after structural QA.

## Decision

The case pack has now rerun four arms: prompt-only, Run 1.5 skill, Run 2.1 full skill, and bad aesthetic memory. Public status remains public blocked.

The useful result is narrow and important: `run2_1_full_skill` shows that data and skill workflow changed generator behavior. It is not yet a public-grade visual presentation. The current deck still leans toward an engineering/product proof because it uses abstract proof boxes instead of realistic slide diffs, visual transformations, and a stronger design climax.

## Next Required Action

Do another content-thickening pass inside the same five layers, with emphasis on visual taste evidence. This does not advance to a later stage or to Run 3.0; it repeats the same product loop with thicker inputs:

- convert tutorial/video observations into concrete before/after layout examples;
- add real slide mini-previews and side-by-side diffs as expected output targets;
- make the full-skill arm prove a visible design transformation, not just a cleaner workflow;
- rerun four arms again, then repeat structural QA, trace refresh, Gemini contact-sheet review, native or manual render inspection, and human approval.
