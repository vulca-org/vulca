"""Integration tests (mock-based).

NOTE: These tests use mocked external services (no real API calls).
For real API integration tests, set GEMINI_API_KEY and run:
    pytest tests/ -m "real_api" -v
"""
